name: CI-CD
on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - run: echo "obter código"
      - run: echo "Executar o docker build"
      - run: echo "Enviar a imagem para o docker Hub"
  CD:
    runs-on: ubuntu-latest
    steps:
      - run: echo "obter código"
      - run: echo "Configurar o kubconfig"
      - run: echo "Executar o Aply"
    
      
